{
  "input": "Create a config for a service named billing-sync. It listens on port 9010, runs 2 replicas, and logs at warn level.",
  "expected": {
    "name": "billing-sync",
    "port": 9010,
    "replicas": 2,
    "log_level": "warn"
  }
}
